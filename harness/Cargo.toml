[package]
name = "adelie-harness"
version = "0.0.0"
edition = "2024"
rust-version = "1.98"
publish = false
description = "Test harness for adelie: sqllogictest runner, engine adapter, crash harness. Never published."
license = "MIT"

[dependencies]
